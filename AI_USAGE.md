# AI 활용 기록

이 저장소의 구현에 AI 코딩 에이전트를 사용했다. 아래는 사용 도구, 적용 범위, 핵심 프롬프트, 그리고 사람이 직접 수행한 검증 기록이다.

## 1. 사용 도구와 모델

| 도구        | 모델               | 주 용도                                                |
| ----------- | ------------------ | ------------------------------------------------------ |
| Claude Code | Claude Fable 5     | 요구사항 감사, 설계와 개선 계획 수립 등 계획 단계 전반 |
| Claude Code | Claude Opus 5 (1M) | 계획에 따른 구현, 리팩터링, 테스트 작성, 문서화        |

터미널 기반 에이전트(Claude Code)만 사용했다. IDE 인라인 자동완성(Copilot 등)이나 별도 채팅 UI는 쓰지 않았다.

## 2. 적용한 작업 범위

### 사람이 담당한 부분

**요구사항 해석.** 과제 원문을 직접 읽고 `docs/requirements/summary.md`로 정리했다. 화면별 요구사항, API 엔드포인트 표, 스키마 필드, 폼 규칙을 표 형태로 재구성한 문서이고, 이후 AI와의 모든 계획 수립은 이 요약본만을 입력으로 삼았다.

원문을 그대로 넘기지 않은 이유는 두 가지다.

1. 요구사항을 읽고 구조화하는 일 자체가 개발자의 역량이라고 판단했다.
2. 출제사의 자료를 AI에게 프롬프트로 전달하거나 저장소에 남기지 않기 위해서다.

**검증 절차 설계.** 요구사항대로 구현됐는지 점검하는 Claude Code 커스텀 스킬을 직접 만들어(공식 `skill-creator` 스킬 사용) 기능 단위마다 실행했다. 무엇을 검증할지 정하는 일은 AI에게 맡기지 않았다.

**모든 변경의 승인.** 아래 4절의 절차대로 건별로 검토하고 승인한 뒤에만 반영했다.

### AI가 담당한 부분

요약본을 입력으로 한 설계 및 개선 계획 수립, 계획 승인 후의 구현과 리팩터링, 테스트 작성, 문서화(`README.md`, `docs/conventions/conventions.md`).

## 3. 핵심 프롬프트 요약

결과물의 방향을 실제로 바꾼 지시 위주로 정리했다.

**① 역할과 판단 기준 부여**

> "당신은 시니어 웹 프론트 엔지니어이자, 새로운 기능의 초기 설계와 확장 가능한 앱 구조를 설계하는 프론트엔드 아키텍트입니다."

작업 지시가 아니라 판단 기준을 먼저 고정하기 위한 프롬프트. 답변이 "요청대로 만들기"에서 "이 구조가 확장 가능한가"로 옮겨갔다.

**② 구현 전 질문 강제**

> "설계 전 파악해야 하는 것 중에 모호한 부분이 있다면 해결될 때까지 나에게 질문해 주세요."

AI가 모호한 요구를 임의로 메우고 진행하는 것을 막았다. 명세가 비어 있는 자리는 질문으로 올라왔고, 그 결정들은 `README.md`의 "자의적 결정 / 가정" 절에 모았다.

**③ 단계별 계획 요구**

> "계획을 단계별(Step-by-step)로 설명해 주세요."

계획을 단계로 쪼개게 해서 사람이 건별로 검토하고 승인할 수 있게 했다.

**④ 요구사항 준수 검증의 도구화**

요약본을 기준으로 구현이 요구사항을 만족하는지 점검하는 커스텀 스킬을 만들어, 기능을 하나 끝낼 때마다 실행했다. 검증을 그때그때의 프롬프트에 맡기지 않고 반복 가능한 절차로 고정하기 위해서다.

## 4. 사람이 최종 검증한 내용

기능 하나를 구현할 때마다 아래 네 단계를 모두 거친 뒤에 커밋했다.

1. **코드 검토와 승인** — 변경 내용을 직접 읽고 의도대로 구현됐는지 확인한 뒤 승인했다. 승인 전에는 반영하지 않았다.
2. **스테이징 단계 재확인** — 커밋 직전 스테이징에 올리면서 변경 사항을 다시 훑어, 의도하지 않은 파일이나 수정이 섞여 들어가지 않았는지 확인했다.
3. **브라우저 동작 확인** — 테스트 통과와 별개로, 실제 화면에서 동작을 눈으로 확인했다.
4. **요구사항 대조** — 커스텀 스킬을 실행해 해당 기능이 요약본의 요구사항을 만족하는지 대조했다.

문서와 코드가 어긋나는 것을 막기 위한 자동 검사도 두었다. 목 계층의 `//- 프로덕션과 다름.` 주석과 `docs/conventions/conventions.md` 3.4절 표의 개수가 파일별로 일치하는지 `scripts/check-mock-markers.mjs`가 검사하고, `pnpm lint`에 포함되어 있다. 규칙을 문서로만 두었을 때 실제로 어긋난 적이 있어 검사를 자동화했다.
